# Git Merge

Git merge is used to combine changes from one branch into another.

It creates a new commit that integrates the histories of both branches.

Example:
git checkout main
git merge feature-login

Merge preserves the full history of both branches.

In some cases, conflicts may occur if the same lines of code were changed differently. These conflicts must be resolved manually.