# Git Stash

Git stash temporarily saves changes that are not ready to be committed.

This allows you to switch branches without losing work.

Example:
git stash
git checkout main

To restore changes:
git stash apply

Stash is useful when you need to quickly change context.