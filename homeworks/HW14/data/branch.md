# Git Branch

A branch in Git is a lightweight movable pointer to a commit.

Branches allow developers to work on different features independently without affecting the main codebase.

Common use cases:
- feature development
- bug fixing
- experimentation

Example:
git branch feature-login
git checkout feature-login

Branches can later be merged back into the main branch.