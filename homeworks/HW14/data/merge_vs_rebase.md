# Merge vs Rebase

Git merge and git rebase are both used to integrate changes from one branch into another.

Merge:
- preserves history
- creates a merge commit
- shows true branching structure

Rebase:
- rewrites history
- creates a linear timeline
- avoids extra merge commits

Merge is safer for shared branches.
Rebase is useful for cleaning up local history before merging.