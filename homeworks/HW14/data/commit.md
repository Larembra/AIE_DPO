# Git Commit

A commit in Git is a snapshot of the repository at a specific point in time.

Each commit contains:
- a unique identifier (hash)
- author information
- a commit message
- changes made

Commits allow developers to save progress and track modifications.

Example command:
git commit -m "Add new feature"

Good commit messages are short but descriptive and explain why a change was made.